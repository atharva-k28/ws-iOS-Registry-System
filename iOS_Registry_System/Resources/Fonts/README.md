# Custom Fonts
# Place any custom .ttf or .otf font files here.
# The app currently uses SF Pro (system default) which doesn't require custom fonts.
# 
# If you add custom fonts:
# 1. Add the font files to this directory
# 2. Add them to Info.plist under "Fonts provided by application"
# 3. Reference them in Typography.swift
